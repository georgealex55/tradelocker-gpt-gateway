export const metadata = { title: "TradeLocker Gateway" };
export default function RootLayout({ children }) {
  return <html><body style={{fontFamily:"Arial",maxWidth:900,margin:"40px auto",padding:20}}>{children}</body></html>;
}
